
CREATE TABLE PRODUCT (
    Product_ID NUMBER PRIMARY KEY,
    Product_Name VARCHAR2(50) NOT NULL,
    Category VARCHAR2(30) NOT NULL,
    Price NUMBER(10,2) CHECK (Price > 0),
    Stock NUMBER CHECK (Stock >= 0)
);

CREATE TABLE CUSTOMER (
    Customer_ID NUMBER PRIMARY KEY,
    Customer_Name VARCHAR2(50) NOT NULL,
    City VARCHAR2(30) NOT NULL,
    Mobile_No VARCHAR2(15) UNIQUE
);

CREATE TABLE ORDERS (
    Order_ID NUMBER PRIMARY KEY,
    Product_ID NUMBER,
    Customer_ID NUMBER,
    Quantity NUMBER CHECK (Quantity > 0),
CONSTRAINT fk_product
        FOREIGN KEY (Product_ID)
        REFERENCES PRODUCT(Product_ID),
CONSTRAINT fk_customer
        FOREIGN KEY (Customer_ID)
        REFERENCES CUSTOMER(Customer_ID)
);



INSERT INTO PRODUCT VALUES (101, 'Mobile', 'Electronics', 25000, 15);
INSERT INTO PRODUCT VALUES (102, 'Mouse', 'Electronics', 700, 50);
INSERT INTO PRODUCT VALUES (103, 'Monitor', 'Electronics', 12000, 10);
INSERT INTO PRODUCT VALUES (104, 'Chair', 'Furniture', 3000, 0);
INSERT INTO PRODUCT VALUES (105, 'Mixer', 'Home Appliances', 4500, 8);

INSERT INTO CUSTOMER VALUES (1, 'Asha', 'Pune', '9876543210');
INSERT INTO CUSTOMER VALUES (2, 'Riya', 'Mumbai', '9876543211');
INSERT INTO CUSTOMER VALUES (3, 'Karan', 'Solapur', '9876543212');
INSERT INTO CUSTOMER VALUES (4, 'Meena', 'Delhi', '9876543213');

INSERT INTO ORDERS VALUES (201, 101, 1, 2);
INSERT INTO ORDERS VALUES (202, 103, 2, 1);
INSERT INTO ORDERS VALUES (203, 105, 3, 3);

COMMIT;


SELECT * 
FROM PRODUCT
WHERE Price > 500;


SELECT *
FROM PRODUCT
WHERE Category = 'Electronics'
AND Price < 50000;


SELECT *
FROM CUSTOMER
WHERE City = 'Pune'
OR City = 'Mumbai';

SELECT *
FROM PRODUCT
WHERE NOT Stock = 0;

SELECT *
FROM PRODUCT
WHERE Price BETWEEN 1000 AND 10000;

SELECT *
FROM CUSTOMER
WHERE City IN ('Pune', 'Solapur', 'Mumbai');

SELECT Product_Name
FROM PRODUCT
WHERE Product_Name LIKE 'M%';

SELECT Customer_Name
FROM CUSTOMER
WHERE Customer_Name LIKE '%a';