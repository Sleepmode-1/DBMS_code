CREATE TABLE HOTEL (
    Hotel_ID NUMBER PRIMARY KEY,
    Hotel_Name VARCHAR2(100) NOT NULL,
    City VARCHAR2(50) NOT NULL,
    Rating NUMBER(2,1) CHECK (Rating BETWEEN 1 AND 5)
);

CREATE TABLE ROOM (
    Room_ID NUMBER PRIMARY KEY,
    Hotel_ID NUMBER NOT NULL,
    Room_Type VARCHAR2(50) NOT NULL,
    Price NUMBER(10,2) CHECK (Price > 0),
    CONSTRAINT fk_hotel
    FOREIGN KEY (Hotel_ID)
    REFERENCES HOTEL(Hotel_ID)
);

CREATE TABLE BOOKING (
    Booking_ID NUMBER PRIMARY KEY,
    Room_ID NUMBER NOT NULL,
    Customer_Name VARCHAR2(100) NOT NULL,
    Checkin_Date DATE NOT NULL,
    CONSTRAINT fk_room
    FOREIGN KEY (Room_ID)
    REFERENCES ROOM(Room_ID)
);
INSERT INTO HOTEL VALUES (1, 'Taj Hotel', 'Mumbai', 5);
INSERT INTO HOTEL VALUES (2, 'Sea View', 'Goa', 4.5);
INSERT INTO HOTEL VALUES (3, 'Royal Stay', 'Pune', 3.8);

INSERT INTO ROOM VALUES (101, 1, 'Deluxe', 5000);
INSERT INTO ROOM VALUES (102, 1, 'Suite', 8000);
INSERT INTO ROOM VALUES (103, 2, 'Standard', 3000);

INSERT INTO BOOKING VALUES (1001, 101, 'Amit Sharma', TO_DATE('10-MAY-2026','DD-MON-YYYY'));
INSERT INTO BOOKING VALUES (1002, 103, 'Priya Patil', TO_DATE('12-MAY-2026','DD-MON-YYYY'));

COMMIT;
CREATE VIEW HOTEL_ROOM_VIEW AS
SELECT H.Hotel_ID,
       H.Hotel_Name,
       H.City,
       H.Rating,
       R.Room_ID,
       R.Room_Type,
       R.Price
FROM HOTEL H
JOIN ROOM R
ON H.Hotel_ID = R.Hotel_ID;

CREATE VIEW BOOKING_VIEW AS
SELECT B.Booking_ID,
       B.Customer_Name,
       B.Checkin_Date,
       R.Room_ID,
       R.Room_Type,
       R.Price
FROM BOOKING B
JOIN ROOM R
ON B.Room_ID = R.Room_ID;

SELECT * FROM HOTEL_ROOM_VIEW;

SELECT * FROM BOOKING_VIEW;

CREATE VIEW HIGH_RATING_HOTELS AS
SELECT *
FROM HOTEL
WHERE Rating > 4;
SELECT * FROM HIGH_RATING_HOTELS;

CREATE VIEW CUSTOMER_ROOM_VIEW AS
SELECT B.Customer_Name,
       R.Room_ID,
       R.Room_Type,
       R.Price
FROM BOOKING B
JOIN ROOM R
ON B.Room_ID = R.Room_ID;
SELECT * FROM CUSTOMER_ROOM_VIEW;

UPDATE HOTEL_ROOM_VIEW
SET Price = 6000
WHERE Room_ID = 101;

COMMIT;

DELETE FROM BOOKING_VIEW
WHERE Booking_ID = 1002;

COMMIT;
DROP VIEW HIGH_RATING_HOTELS;