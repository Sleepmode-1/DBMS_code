CREATE TABLE EMPLOYEE (
    Emp_ID NUMBER PRIMARY KEY,
    Emp_Name VARCHAR2(50) NOT NULL,
    Department VARCHAR2(50) NOT NULL,
    Salary NUMBER(10,2) CHECK (Salary > 0)
);

CREATE TABLE PROJECT (
    Project_ID NUMBER PRIMARY KEY,
    Project_Name VARCHAR2(50) NOT NULL,
    Budget NUMBER(12,2) CHECK (Budget > 0)
);

CREATE TABLE EMP_PROJECT (
    Emp_ID NUMBER,
    Project_ID NUMBER,
    Working_Hours NUMBER(5,2) CHECK (Working_Hours >= 0),
    PRIMARY KEY (Emp_ID, Project_ID),
CONSTRAINT fk_emp
        FOREIGN KEY (Emp_ID)
        REFERENCES EMPLOYEE(Emp_ID),
CONSTRAINT fk_project
        FOREIGN KEY (Project_ID)
        REFERENCES PROJECT(Project_ID)
);
INSERT INTO EMPLOYEE VALUES (1, 'Amit', 'IT', 60000);
INSERT INTO EMPLOYEE VALUES (2, 'Neha', 'HR', 45000);
INSERT INTO EMPLOYEE VALUES (3, 'Raj', 'IT', 70000);
INSERT INTO EMPLOYEE VALUES (4, 'Sneha', 'Finance', 55000);
INSERT INTO EMPLOYEE VALUES (5, 'Karan', 'HR', 50000);


INSERT INTO PROJECT VALUES (101, 'Website Development', 500000);
INSERT INTO PROJECT VALUES (102, 'Mobile App', 750000);
INSERT INTO PROJECT VALUES (103, 'ERP System', 1000000);

INSERT INTO EMP_PROJECT VALUES (1, 101, 120);
INSERT INTO EMP_PROJECT VALUES (2, 101, 80);
INSERT INTO EMP_PROJECT VALUES (3, 102, 150);
INSERT INTO EMP_PROJECT VALUES (4, 103, 200);
INSERT INTO EMP_PROJECT VALUES (5, 101, 90);
INSERT INTO EMP_PROJECT VALUES (1, 102, 100);

COMMIT;
SELECT Department, COUNT(*) AS Total_Employees
FROM EMPLOYEE
GROUP BY Department;

SELECT Department, AVG(Salary) AS Average_Salary
FROM EMPLOYEE
GROUP BY Department;

SELECT Department, MAX(Salary) AS Maximum_Salary
FROM EMPLOYEE
GROUP BY Department;

SELECT Department, MIN(Salary) AS Minimum_Salary
FROM EMPLOYEE
GROUP BY Department;

SELECT Department, SUM(Salary) AS Total_Salary
FROM EMPLOYEE
GROUP BY Department;

SELECT Project_ID, SUM(Working_Hours) AS Total_Working_Hours
FROM EMP_PROJECT
GROUP BY Project_ID;

SELECT Department, AVG(Salary) AS Average_Salary
FROM EMPLOYEE
GROUP BY Department
HAVING AVG(Salary) > 50000;

SELECT Project_ID, COUNT(Emp_ID) AS Total_Employees
FROM EMP_PROJECT
GROUP BY Project_ID
HAVING COUNT(Emp_ID) > 2;

SELECT MAX(Budget) AS Highest_Budget
FROM PROJECT;

SELECT MIN(Salary) AS Lowest_Salary
FROM EMPLOYEE;