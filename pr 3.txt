CREATE TABLE BOOK (
    Book_ID NUMBER(5) PRIMARY KEY,
    Book_Name VARCHAR2(50) NOT NULL,
    Author VARCHAR2(50) NOT NULL,
    Price NUMBER(8,2) CHECK (Price > 0)
);

DESC BOOK;

CREATE TABLE MEMBER (
    Member_ID NUMBER(5) PRIMARY KEY,
    Member_Name VARCHAR2(50) NOT NULL,
    City VARCHAR2(30),
    Phone_No NUMBER(10) UNIQUE
);

DESC MEMBER;

CREATE TABLE ISSUE (
    Issue_ID NUMBER(5) PRIMARY KEY,
    Book_ID NUMBER(5),
    Member_ID NUMBER(5),
    Issue_Date DATE
);
DESC ISSUE;

ALTER TABLE ISSUE
ADD CONSTRAINT FK_BOOK
FOREIGN KEY (Book_ID)
REFERENCES BOOK(Book_ID);

ALTER TABLE ISSUE
ADD CONSTRAINT FK_MEMBER
FOREIGN KEY (Member_ID)
REFERENCES MEMBER(Member_ID);
DESC ISSUE;

SELECT * FROM USER_CONSTRAINTS
WHERE TABLE_NAME='ISSUE';

INSERT INTO BOOK VALUES (101, 'SQL Basics', 'James', 450);
INSERT INTO BOOK VALUES (102, 'Advanced SQL', 'Scott', 700);
INSERT INTO BOOK VALUES (103, 'Python Programming', 'Guido', 550);
INSERT INTO BOOK VALUES (104, 'Java Fundamentals', 'Oracle', 300);

INSERT INTO MEMBER VALUES (1, 'Rahul', 'Pune', '9876543210');
INSERT INTO MEMBER VALUES (2, 'Sneha', 'Mumbai', '9876501234');

INSERT INTO ISSUE VALUES (11, 102, 1, SYSDATE);
INSERT INTO ISSUE VALUES (12, 103, 2, SYSDATE);

SELECT *
FROM BOOK
WHERE Price > (SELECT AVG(Price) FROM BOOK);

SELECT DISTINCT M.Member_Name
FROM MEMBER M
JOIN ISSUE I
ON M.Member_ID = I.Member_ID
JOIN BOOK B
ON I.Book_ID = B.Book_ID
WHERE B.Price > 500;

SELECT *
FROM BOOK
WHERE Price = (SELECT MAX(Price) FROM BOOK);

SELECT *
FROM BOOK
WHERE Book_ID NOT IN
      (SELECT Book_ID FROM ISSUE);

SELECT MAX(Price) AS Second_Highest_Price
FROM BOOK
WHERE Price <
      (SELECT MAX(Price) FROM BOOK);

